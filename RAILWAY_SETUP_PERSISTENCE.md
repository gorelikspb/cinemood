# 🔧 Настройка Persistent Storage для Railway

## Проблема:
База данных SQLite теряется при каждом перезапуске Railway, так как находится в ephemeral storage.

## ✅ Решение: Railway Persistent Volume

### Шаг 1: Создать Volume в Railway

1. Откройте Railway Dashboard → ваш проект
2. Перейдите в **Settings** → **Volumes**
3. Нажмите **Create Volume**
4. Заполните:
   - **Name**: `database-storage`
   - **Mount Path**: `/data`
   - **Size**: 1 GB (достаточно для SQLite)
5. Нажмите **Create**

### Шаг 2: Добавить переменную окружения

1. Перейдите в **Variables**
2. Добавьте новую переменную:
   - **Name**: `DATABASE_PATH`
   - **Value**: `/data/database.sqlite`
3. Нажмите **Add**

### Шаг 3: Перезапустить сервис

После создания Volume и добавления переменной:
1. Railway автоматически перезапустит сервис
2. База данных будет сохраняться в `/data/database.sqlite`
3. Данные будут сохраняться между перезапусками

## ✅ Проверка:

После перезапуска проверьте логи Railway:
```
📁 Database path: /data/database.sqlite
📁 DATABASE_PATH env: /data/database.sqlite
```

Если видите `/data/database.sqlite` - всё настроено правильно!

## 📝 Примечание:

- Первый запуск создаст новую базу данных в `/data/database.sqlite`
- Данные из старой базы (если были) нужно будет восстановить вручную или через API экспорт
- Volume сохраняется даже при перезапусках и редеплоях


