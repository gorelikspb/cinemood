# ⚠️ ПРОБЛЕМА: База данных теряется на Railway

## Проблема:

SQLite база данных на Railway находится в **ephemeral storage** (временное хранилище) и **теряется при каждом перезапуске/редеплое** контейнера.

Это нормальное поведение для Railway - файлы в `/app` не сохраняются между перезапусками.

## 🔍 Что происходит:

1. Вы отправляете email/feedback → данные сохраняются в базу
2. Railway перезапускает контейнер (автоматически или при деплое)
3. База данных удаляется → данные теряются
4. Новая база создается пустой

## ✅ Решения:

### Вариант 1: Использовать Railway Persistent Volume (рекомендуется)

1. Railway Dashboard → ваш проект
2. **Settings** → **Volumes**
3. **Create Volume**
4. Name: `database-storage`
5. Mount Path: `/data`
6. **Create**

Затем обновите `server/database.js`:
```javascript
const dbPath = process.env.DATABASE_PATH || path.join('/data', 'database.sqlite');
```

И добавьте в Railway Variables:
- `DATABASE_PATH=/data/database.sqlite`

### Вариант 2: Регулярно экспортировать данные через API

Просто периодически скачивайте данные через API endpoints:
- `/api/emails?format=csv`
- `/api/feedback?format=csv`

### Вариант 3: Использовать PostgreSQL (для production)

Railway предоставляет бесплатный PostgreSQL. Можно мигрировать с SQLite на PostgreSQL.

---

## 🎯 Что делать сейчас:

Если нужно сохранить данные:
1. Используйте API endpoints для экспорта (`/api/emails?format=csv`, `/api/feedback?format=csv`)
2. Или настройте Persistent Volume (Вариант 1)

Для тестирования:
- Данные сохраняются временно, пока контейнер не перезапустится
- Это нормально для разработки

---

## 📝 Примечание:

Видно, что Google Sheets webhook URL настроен (length: 112). Если настроить Google Sheets правильно, данные будут сохраняться там автоматически, даже если база данных теряется.

