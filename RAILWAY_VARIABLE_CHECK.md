# ✅ ПРОВЕРКА: Почему Railway не отправляет в Google Sheets

## ✅ Хорошая новость:
Google Apps Script работает! Postman успешно добавил email в Google Sheets.

## ❌ Проблема:
Railway не отправляет запросы в Google Sheets.

## 🔍 Что проверить:

### 1. Переменная в Railway:
- Railway Dashboard → ваш проект → **Variables**
- Есть ли `GOOGLE_SHEETS_WEBHOOK_URL`?
- Если нет → **ДОБАВЬТЕ:**
  - **Name**: `GOOGLE_SHEETS_WEBHOOK_URL`
  - **Value**: ваш Google Apps Script URL (из Postman, который работает)

### 2. После добавления переменной:
- Railway автоматически перезапустит сервер
- Подождите 1-2 минуты
- Откройте логи Railway → **Deployments** → последний deployment → **View Logs**
- Должна появиться строка:
  ```
  🔍 GOOGLE_SHEETS_WEBHOOK_URL check: { exists: true, length: 100+, preview: 'https://script.google.com/macros/s/AKfyc...' }
  ```

### 3. Отправьте тестовый email:
- Через ваше приложение отправьте тестовый email
- Проверьте логи Railway
- Должны появиться строки:
  - `📧 New email saved: ...`
  - `📤 About to call sendToGoogleSheets for new email`
  - `🔍 sendToGoogleSheets called: ...`
  - `📤 Sending email to Google Sheets: ...`
  - `✅ Email sent to Google Sheets successfully`

### 4. Если переменная есть, но логи не появляются:
- Возможно, код еще не задеплоился
- Подождите еще 2-3 минуты
- Проверьте время последнего deployment

---

## 🎯 Следующий шаг:

**Добавьте переменную `GOOGLE_SHEETS_WEBHOOK_URL` в Railway Variables** (если еще не добавили), и после перезапуска отправьте тестовый email через приложение.

Что покажет проверка переменной в Railway Variables?

