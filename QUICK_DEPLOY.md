# 🚀 Быстрый деплой на Netlify (5 минут)

## ✅ Что уже готово:

- ✅ Файл `netlify.toml` создан и настроен
- ✅ Все изменения закоммичены в git
- ✅ Проект готов к деплою

## 📋 Что нужно сделать (пошагово):

### Шаг 1: Отправьте код на GitHub

Если у вас еще нет репозитория на GitHub:

```bash
# 1. Создайте новый репозиторий на github.com (New repository)
# 2. Назовите его например: "cinemood"
# 3. НЕ создавайте README, .gitignore (уже есть)
# 4. Скопируйте команды которые GitHub покажет (или используйте ниже)
```

Если репозиторий уже существует:

```bash
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
git branch -M main
git push -u origin main
```

**Или если репозиторий уже подключен:**

```bash
git push
```

---

### Шаг 2: Подключите Netlify к GitHub

1. **Зайдите на https://app.netlify.com**
2. **Нажмите "Sign up"** (если нет аккаунта) или **"Log in"**
3. **Войдите через GitHub** (кнопка "Sign up with GitHub")
4. **Нажмите "Add new site"** → **"Import an existing project"**
5. **Выберите "Deploy with GitHub"**
6. **Авторизуйте Netlify** доступ к вашему GitHub (нажмите "Authorize netlify")
7. **Выберите ваш репозиторий** из списка
8. **Настройки автоматически подтянутся из `netlify.toml`!**
   - Base directory: `client` ✅
   - Build command: `npm install && npm run build` ✅
   - Publish directory: `build` ✅
9. **Нажмите "Deploy site"**

**⏱️ Подождите 2-3 минуты** - Netlify соберет проект

---

### Шаг 3: Получите домен!

После деплоя вы увидите:
- ✅ **"Site is live"**
- 🌐 **Ваш домен**: `https://cinemood-12345.netlify.app` (или похожий)

**Скопируйте этот домен!**

---

### Шаг 4: Настройте Google Analytics

1. **Откройте Google Analytics**
2. **Admin** → **Data Streams** → выберите ваш поток
3. **Нажмите на поток** → **Edit**
4. **Website URL**: вставьте ваш домен от Netlify (например: `https://cinemood-12345.netlify.app`)
5. **Сохраните**

---

### Шаг 5: Добавьте переменные окружения в Netlify

1. В Netlify Dashboard: **Site settings** → **Environment variables**
2. Нажмите **"Add variable"**
3. Добавьте:
   - **Key**: `REACT_APP_GA4_MEASUREMENT_ID`
   - **Value**: `G-XXXXXXXXXX` (ваш Measurement ID из Google Analytics)
4. Нажмите **"Save"**
5. **Trigger deploy** → **Deploy site** (чтобы применить переменные)

---

### Шаг 6: Проверьте работу!

1. Откройте ваш домен (например: `https://cinemood-12345.netlify.app`)
2. Выполните какое-то действие (добавьте фильм)
3. Откройте Google Analytics → **Realtime** → **Events**
4. Вы должны увидеть события!

---

## 🎉 Готово!

Теперь у вас есть:
- ✅ Бесплатный домен на Netlify
- ✅ Автоматический деплой при каждом `git push`
- ✅ Работающий Google Analytics
- ✅ HTTPS (SSL) автоматически

---

## 🔄 Автоматический деплой

Теперь при каждом `git push` в main ветку, Netlify автоматически:
1. Запустит сборку
2. Задеплоит новую версию
3. Обновит сайт за 2-3 минуты

**Ничего дополнительно делать не нужно!** 🚀

