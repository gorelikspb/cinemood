# 🔍 ПРОВЕРКА: Почему логи не появляются в Railway

## Возможные причины:

1. **Логи обновляются с задержкой** (30-60 секунд)
2. **Нужно обновить страницу логов** в Railway
3. **Переменная не установлена** - тогда функция просто возвращается без логирования

## Что проверить:

### 1. Проверьте переменную в Railway:
- Railway Dashboard → ваш проект → **Variables**
- Есть ли `GOOGLE_SHEETS_WEBHOOK_URL`?
- Если нет → добавьте и подождите перезапуска

### 2. Проверьте логи Railway правильно:
- Railway Dashboard → ваш проект
- **Deployments** → выберите **ПОСЛЕДНИЙ** deployment (самый верхний)
- **View Logs** (или кнопка с иконкой логов)
- **Обновите страницу** (F5)
- Попробуйте **Live Logs** если есть такая опция

### 3. Проверьте время деплоя:
- Посмотрите на время последнего deployment
- Если код был закоммичен только что, подождите 1-2 минуты
- Railway должен автоматически задеплоить новую версию

### 4. Добавьте тестовый лог при старте:
Откройте `server/index.js` и добавьте в конец (после `app.listen`):

```javascript
console.log('🔍 Checking GOOGLE_SHEETS_WEBHOOK_URL:', {
  exists: !!process.env.GOOGLE_SHEETS_WEBHOOK_URL,
  length: process.env.GOOGLE_SHEETS_WEBHOOK_URL?.length || 0,
  preview: process.env.GOOGLE_SHEETS_WEBHOOK_URL?.substring(0, 50) || 'NOT SET'
});
```

Этот лог появится сразу при старте сервера и покажет, читается ли переменная.

### 5. Альтернатива: Проверьте через API напрямую

Если логи не помогают, можно добавить endpoint для проверки переменных (только для отладки):

```javascript
// Временно добавьте в server/routes/emails.js (для отладки)
router.get('/debug', (req, res) => {
  res.json({
    webhookUrlExists: !!process.env.GOOGLE_SHEETS_WEBHOOK_URL,
    webhookUrlPreview: process.env.GOOGLE_SHEETS_WEBHOOK_URL?.substring(0, 50) || 'NOT SET'
  });
});
```

Потом откройте: `https://ваш-railway-url.up.railway.app/api/emails/debug`

---

## Самый быстрый способ проверить:

1. Railway Dashboard → Variables
2. Убедитесь, что `GOOGLE_SHEETS_WEBHOOK_URL` есть
3. Если нет → добавьте (URL должен заканчиваться на `/exec`)
4. Подождите 1-2 минуты (Railway перезапустит сервер)
5. Отправьте тестовый email
6. Проверьте логи еще раз (обновите страницу)

Что показывает переменная `GOOGLE_SHEETS_WEBHOOK_URL` в Railway Variables?

