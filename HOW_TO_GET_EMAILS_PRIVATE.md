# 📧 Как получить email адреса пользователей (ПРИВАТНО)

⚠️ **ЭТОТ ФАЙЛ НЕ ДОЛЖЕН ПОПАСТЬ В GITHUB!**

## 🔒 Защищенный API endpoint

API endpoint `/api/emails` теперь защищен токеном авторизации.

### Настройка токена:

1. В Railway Dashboard → **Variables** → добавьте:
   - **Name**: `ADMIN_TOKEN`
   - **Value**: придумайте сложный токен (например: `super-secret-token-2024`)

2. Сохраните переменную

### Получение email:

```bash
# Локально
curl -H "Authorization: Bearer ваш-токен" http://localhost:5000/api/emails

# На Railway
curl -H "Authorization: Bearer ваш-токен" https://ваш-railway-url.up.railway.app/api/emails?format=csv > emails.csv
```

### Или в браузере (через расширение):

1. Установите расширение для браузера (например, "ModHeader" для Chrome)
2. Добавьте заголовок: `Authorization: Bearer ваш-токен`
3. Откройте: `https://ваш-railway-url.up.railway.app/api/emails`

---

## 📋 Альтернатива: Прямой доступ к базе данных

Если у вас есть доступ к Railway файлам:

```bash
# Используйте Railway CLI или SSH доступ
cd server
sqlite3 database.sqlite

SELECT id, email, created_at FROM emails ORDER BY created_at DESC;
```

---

## ⚠️ Важно:

- **НЕ коммитьте этот файл в git!**
- **НЕ делитесь токеном публично!**
- Храните токен в переменных окружения Railway

