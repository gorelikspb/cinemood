# 🔄 Инструкция по сбросу и перезапуску приложения

## 🛑 Остановка серверов

### Быстрая остановка (через скрипт):
```powershell
.\restart.ps1
```

### Ручная остановка:
- Нажмите `Ctrl+C` в терминалах, где запущены серверы
- Или закройте эти окна терминала

## 🧹 Полный сброс данных

### 1. Сброс базы данных (опционально)

Если нужно полностью очистить все данные:

**В PowerShell:**
```powershell
cd server
node reset-db.js
```

### 2. Очистка кэша и зависимости (если проблемы с установкой)

**В PowerShell:**
```powershell
# Удалить node_modules в корне проекта
Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue

# Удалить node_modules в client
Remove-Item -Recurse -Force client/node_modules -ErrorAction SilentlyContinue

# Удалить node_modules в server
Remove-Item -Recurse -Force server/node_modules -ErrorAction SilentlyContinue

# Переустановить зависимости
npm run install-all
```

### 3. Очистка localStorage в браузере

#### Вариант 1: Через консоль браузера (быстро)
1. Откройте приложение в браузере (http://localhost:3000)
2. Нажмите `F12` (или `Ctrl+Shift+I`) чтобы открыть Developer Tools
3. Перейдите на вкладку "Console"
4. Введите следующий код:

```javascript
localStorage.clear();
```

5. Нажмите `Enter`
6. Обновите страницу (`F5`)

#### Вариант 2: Удалить только данные Rewatch
В консоли браузера выполните:

```javascript
localStorage.removeItem('rewatch-language');
localStorage.removeItem('rewatch-email');
localStorage.removeItem('rewatch-watchlist');
```

#### Что будет очищено:
- 💬 Выбранный язык (EN/RU)
- 📧 Зарегистрированный email
- 📝 Добавленные в watchlist фильмы
- 📺 Все сохраненные фильмы из базы данных (если выполнен reset-db.js)

## 🚀 Перезапуск серверов

### Способ 1: Использование готовых скриптов (рекомендуется)

**Откройте ДВА отдельных окна терминала (PowerShell) в корне проекта (`D:\dev\watched`)**

#### Терминал 1 - Backend сервер:
```powershell
.\start-server.ps1
```

#### Терминал 2 - Frontend клиент:
```powershell
.\start-client.ps1
```

#### Остановка всех процессов:
```powershell
.\restart.ps1
```

Этот скрипт остановит процессы на портах 3000 и 5000, затем вы сможете запустить серверы заново.

### Способ 2: Ручной запуск

**Откройте ДВА отдельных окна терминала (PowerShell)**

#### Терминал 1 - Backend сервер:
```powershell
cd D:\dev\watched\server
npm start
```

Вы должны увидеть:
```
🎬 Rewatch server running on port 5000
📊 Environment: development
```

#### Терминал 2 - Frontend клиент:
```powershell
cd D:\dev\watched\client
npm start
```

Вы должны увидеть:
```
Compiled successfully!
You can now view rewatch in the browser.
  Local:            http://localhost:3000
```

### Альтернатива: Использование одного терминала с фоновыми процессами

Если хотите запустить оба сервера из одного терминала:

**Для Backend:**
```powershell
cd D:\dev\watched\server
Start-Job -ScriptBlock { Set-Location "D:\dev\watched\server"; npm start }
```

**Для Frontend:**
```powershell
cd D:\dev\watched\client
Start-Job -ScriptBlock { Set-Location "D:\dev\watched\client"; npm start }
```

⚠️ **Не рекомендуется** - сложнее видеть логи и останавливать процессы

## 🔍 Проверка работоспособности

1. Откройте http://localhost:3000 в браузере
2. Откройте консоль разработчика (F12)
3. Проверьте, что нет ошибок `ERR_CONNECTION_REFUSED`
4. Проверьте Network tab - должны быть успешные запросы к `localhost:5000/api/*`

## 🐛 Решение проблем

### Сервер не запускается

1. **Проверьте порт 5000:**
   ```powershell
   netstat -ano | findstr :5000
   ```
   Если порт занят, найдите процесс и завершите его или измените PORT в `.env`

2. **Проверьте .env файл:**
   Убедитесь, что в `server/.env` есть:
   ```
   TMDB_API_KEY=your_actual_api_key
   PORT=5000
   ```

3. **Переустановите зависимости:**
   ```powershell
   cd server
   Remove-Item -Recurse -Force node_modules
   npm install
   ```

### Клиент не запускается

1. **Проверьте порт 3000:**
   ```powershell
   netstat -ano | findstr :3000
   ```

2. **Очистите кэш:**
   ```powershell
   cd client
   Remove-Item -Recurse -Force node_modules
   npm install
   ```

3. **Очистите кэш сборки:**
   ```powershell
   cd client
   Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
   npm start
   ```

## 📝 Быстрая справка по командам PowerShell

| Действие | Команда |
|----------|---------|
| Перейти в папку | `cd путь\к\папке` |
| Запустить сервер | `npm start` |
| Остановить процесс | `Ctrl+C` |
| Проверить порт | `netstat -ano | findstr :5000` |
| Удалить папку | `Remove-Item -Recurse -Force имя_папки` |

## ✅ Чеклист после перезапуска

- [ ] Backend сервер запущен и показывает сообщение о порте 5000
- [ ] Frontend клиент запущен и открыт на localhost:3000
- [ ] Нет ошибок в консоли браузера
- [ ] Страница загружается корректно
- [ ] API запросы проходят успешно (проверить Network tab в DevTools)
