# 🔍 ДИАГНОСТИКА: Почему email не попадает в Google Sheets

## Шаг 1: Проверьте логи Railway

1. Откройте Railway Dashboard → ваш проект
2. **Deployments** → выберите последний деплой
3. **View Logs**
4. Найдите логи при отправке email:
   - ✅ Должно быть: `📤 Sending email to Google Sheets: ...`
   - ✅ Должно быть: `✅ Email sent to Google Sheets successfully`
   - ❌ Если есть: `⚠️ GOOGLE_SHEETS_WEBHOOK_URL not configured` → переменная не установлена
   - ❌ Если есть: `❌ Failed to send to Google Sheets` → проблема с запросом

## Шаг 2: Проверьте переменную окружения

1. Railway Dashboard → ваш проект → **Variables**
2. Убедитесь, что есть переменная:
   - **Name**: `GOOGLE_SHEETS_WEBHOOK_URL`
   - **Value**: ваш Web App URL из Google Apps Script
3. Если переменной нет → добавьте её
4. Если переменная есть → перезапустите сервер (Railway делает это автоматически)

## Шаг 3: Проверьте Google Apps Script

1. Откройте Google Sheets → **Extensions** → **Apps Script**
2. **View** → **Execution log**
3. Если видите ошибки → исправьте их
4. Если логов нет → возможно запрос не доходит

## Шаг 4: Проверьте Web App URL

1. Убедитесь, что Web App URL правильный:
   ```
   https://script.google.com/macros/s/AKfycby.../exec
   ```
2. URL должен заканчиваться на `/exec`
3. Откройте URL в браузере → должно появиться сообщение об ошибке (это нормально для GET запросов)

## Шаг 5: Проверьте права доступа

1. Google Apps Script → **Deploy** → **Manage deployments**
2. Убедитесь, что:
   - **Execute as**: Me
   - **Who has access**: Anyone

## Шаг 6: Тестовый запрос

Попробуйте отправить тестовый запрос вручную через curl или Postman:

```bash
curl -X POST "https://script.google.com/macros/s/ВАШ_URL/exec" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "timestamp": "2024-01-15T12:00:00Z",
    "source": "test",
    "deviceType": "desktop",
    "browser": "Chrome",
    "os": "Windows",
    "language": "en"
  }'
```

Если запрос проходит, но email не появляется в таблице → проблема в Google Apps Script.

## ⚠️ Частые проблемы:

1. **Переменная не установлена** → добавьте `GOOGLE_SHEETS_WEBHOOK_URL` в Railway
2. **Неправильный URL** → проверьте, что URL заканчивается на `/exec`
3. **Права доступа** → Web App должен быть доступен для "Anyone"
4. **Ошибка в Google Apps Script** → проверьте логи выполнения
5. **Несоответствие количества колонок** → убедитесь, что заголовки соответствуют количеству полей в `appendRow`

## ✅ После исправления:

1. Отправьте тестовый email через приложение
2. Проверьте логи Railway
3. Проверьте Google Sheets → должна появиться новая строка

