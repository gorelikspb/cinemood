# 📊 Настройка Google Analytics 4 (Бесплатно!)

Google Analytics 4 **полностью бесплатен** и идеально подходит для веб-приложений!

## 🚀 Быстрая настройка

### 1. Создание аккаунта Google Analytics

1. Перейдите на [Google Analytics](https://analytics.google.com/)
2. Нажмите **"Начать измерение"** или войдите в существующий аккаунт
3. Создайте новый аккаунт (если нужно)

### 2. Создание свойства

1. Нажмите **"Создать свойство"**
2. Заполните форму:
   - **Название свойства**: `Cinemood` (любое имя)
   - **Часовой пояс**: выберите ваш
   - **Валюта**: ваша валюта
3. Нажмите **"Далее"**

### 3. Настройка потока данных (Data Stream)

1. Выберите платформу: **"Веб"** (Web)
2. Заполните:
   - **URL веб-сайта**: `http://localhost:3000` (для разработки)
   - **Название потока**: `Cinemood Web`
3. Нажмите **"Создать поток"**
4. **Скопируйте Measurement ID** (формат: `G-XXXXXXXXXX`) - он понадобится!

### 4. Настройка в проекте

#### Вариант A: Через переменную окружения (рекомендуется)

Создайте файл `client/.env`:

```env
REACT_APP_GA4_MEASUREMENT_ID=G-XXXXXXXXXX
```

#### Вариант B: Прямо в коде

1. Откройте `client/public/index.html`
2. Найдите `G-XXXXXXXXXX` (2 места)
3. Замените на ваш Measurement ID

Или в `client/src/config/analytics.ts`:
```typescript
measurementId: process.env.REACT_APP_GA4_MEASUREMENT_ID || 'G-ВАШ_ID',
```

### 5. Проверка работы

1. Перезапустите клиент: `.\start-client.ps1`
2. Откройте `http://localhost:3000`
3. Выполните действия (добавьте фильм, эмоцию и т.д.)
4. Откройте Google Analytics → **Reports** → **Realtime**
5. События появятся в течение нескольких секунд!

## 📱 Где смотреть аналитику

### Realtime отчеты (для немедленной проверки):

1. Google Analytics → **Reports** → **Realtime**
2. Вы увидите активных пользователей и события в реальном времени
3. Раздел **"Events"** покажет все кастомные события

### События (Events):

1. Google Analytics → **Reports** → **Engagement** → **Events**
2. Увидите все отслеживаемые события:
   - `AddFilm` - добавление фильма
   - `AddEmotion` - добавление эмоции
   - `OpenSimilar` - открытие похожего фильма
   - `EmailSubmitted` - отправка email
   - `InstalledPWA` - установка PWA
   - И другие...

### Отчеты по поведению пользователей:

1. **User Acquisition** - откуда приходят пользователи
2. **User Engagement** - что делают пользователи
3. **User Retention** - как часто возвращаются

## 🎯 Отслеживаемые события

- **AddFilm** - добавление фильма в дневник (movieId, rating, emotionsCount)
- **AddEmotion** - добавление эмоции к фильму (emotionType)
- **OpenSimilar** - открытие похожего фильма (movieId, similarMovieId, similarMovieTitle)
- **EmailSubmitted** - отправка email (source или hasMovieTitle)
- **InstallPromptShown** - показ промпта установки PWA
- **InstalledPWA** - установка PWA
- **AddToWatchlist** - добавление в список желаний (movieId)
- **RemoveFromWatchlist** - удаление из списка желаний (movieId)
- **FeedbackSubmitted** - отправка отзыва (hasEmail, messageLength)

**+ Автоматическое отслеживание переходов между страницами (для SPA)**

## 🔧 Отключение аналитики

Если нужно временно отключить:

1. В `client/src/config/analytics.ts` установите:
   ```typescript
   enabled: false,
   ```
2. Или через переменную окружения:
   ```env
   REACT_APP_ANALYTICS_ENABLED=false
   ```

## 💰 Планы Google Analytics

- **GA4 Free**: **Полностью бесплатно**, неограниченное количество событий и пользователей
- **GA4 360**: Для крупных корпораций (обычно не нужно)

**Google Analytics 4 полностью бесплатен!** 🎉

## 🔒 Конфиденциальность

Google Analytics соответствует GDPR и позволяет настраивать согласие на cookies через **Consent Mode**.

## 🌐 Настройка для production

Когда будете деплоить:

1. В Google Analytics создайте новый **Data Stream** для production домена
2. Получите новый Measurement ID
3. Обновите `REACT_APP_GA4_MEASUREMENT_ID` в production `.env`

Или используйте один Measurement ID для обоих (localhost и production).

## ❓ Проблемы?

- **События не появляются?** 
  - Проверьте, что Measurement ID правильно указан в `index.html`
  - Откройте консоль браузера - должны быть логи `📊 Track event:`
  - Используйте **Realtime** отчет для немедленной проверки

- **Ошибка в консоли?**
  - Убедитесь, что скрипт gtag.js загружается (проверьте Network tab)
  - Проверьте, что Measurement ID правильный (формат: `G-XXXXXXXXXX`)

- **Не работает в dev режиме?**
  - GA4 работает и на localhost - просто укажите правильный Measurement ID
