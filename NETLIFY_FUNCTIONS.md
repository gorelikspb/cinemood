# 🚀 Деплой Backend на Netlify Functions

## ⚠️ Важно: Это сложнее, чем Railway!

Netlify Functions требуют переписать Express приложение в serverless функции. Если хотите простое решение - используйте **Railway** (см. `RAILWAY_DEPLOY.md`).

---

## Как это работает

Netlify Functions - это serverless функции (как AWS Lambda). Вместо одного Express сервера нужно создать отдельную функцию для каждого endpoint.

---

## Структура проекта

```
netlify/
  functions/
    movies.js      # GET /api/movies, POST /api/movies
    movie.js       # GET /api/movies/:id, PUT /api/movies/:id, DELETE /api/movies/:id
    emotions.js    # POST /api/emotions, DELETE /api/emotions/:id
    stats.js       # GET /api/stats/overview, GET /api/stats/monthly
    emails.js      # POST /api/emails
    feedback.js    # POST /api/feedback, GET /api/feedback
```

---

## Проблемы

1. **SQLite на Netlify**: Файловая система в serverless функциях доступна только для чтения во время выполнения. Нужна внешняя база данных (PostgreSQL, MongoDB и т.д.).

2. **Переписывание кода**: Каждый route нужно переписать в отдельную функцию.

3. **Cold start**: Первый запрос может быть медленным (500ms-2s).

---

## Рекомендация

**Для вашего проекта лучше Railway:**
- ✅ Не нужно переписывать код
- ✅ SQLite работает из коробки
- ✅ Проще настроить
- ✅ Быстрее работает

**Netlify Functions подходят если:**
- ❌ Нужна максимальная масштабируемость
- ❌ Готовы переписать на serverless
- ❌ Готовы перейти на PostgreSQL/MongoDB

---

## Если всё же хотите Netlify Functions

### Шаг 1: Установите Netlify CLI

```bash
npm install -g netlify-cli
```

### Шаг 2: Создайте структуру функций

Каждый endpoint = отдельная функция в `netlify/functions/`.

### Шаг 3: Мигрируйте на PostgreSQL

SQLite не работает в Netlify Functions, нужна внешняя БД.

### Шаг 4: Перепишите routes

Каждый route из Express → отдельная serverless функция.

---

## Вывод

**Для вашего случая Railway - лучший выбор!** 🚀

См. `RAILWAY_DEPLOY.md` для простой инструкции.

