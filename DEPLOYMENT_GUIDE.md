# 🚀 Гайд по деплою и получению бесплатного домена

## Варианты бесплатного хостинга с доменом

### 1. Netlify (Рекомендуется) ⭐

**Что дает:**
- Домен: `ваш-проект.netlify.app`
- Бесплатный SSL (HTTPS)
- Автоматический деплой из GitHub
- Поддержка React/SPA
- Можно подключить свой домен позже

**Шаги:**
1. Зарегистрируйтесь на [Netlify](https://www.netlify.com/)
2. Подключите GitHub репозиторий
3. Настройки деплоя:
   - **Build command**: `cd client && npm run build`
   - **Publish directory**: `client/build`
4. Нажмите "Deploy"
5. Получите домен вида `cinemood-123.netlify.app`

---

### 2. Vercel

**Что дает:**
- Домен: `ваш-проект.vercel.app`
- Бесплатный SSL
- Автодеплой из GitHub
- Поддержка React

**Шаги:**
1. Зарегистрируйтесь на [Vercel](https://vercel.com/)
2. Import проект из GitHub
3. Настройки:
   - **Framework Preset**: Create React App
   - **Root Directory**: `client`
4. Deploy

---

### 3. Cloudflare Pages

**Что дает:**
- Домен: `ваш-проект.pages.dev`
- Бесплатный SSL
- Быстрая CDN

**Шаги:**
1. Зарегистрируйтесь на [Cloudflare](https://www.cloudflare.com/)
2. Pages → Create a project
3. Подключите GitHub
4. Настройки:
   - **Build command**: `cd client && npm run build`
   - **Build output directory**: `client/build`

---

## 🎯 Рекомендация: Netlify

**Почему Netlify:**
- ✅ Самый простой в использовании
- ✅ Отличная документация на русском
- ✅ Бесплатный план щедрый (100GB bandwidth)
- ✅ Легко настроить переменные окружения
- ✅ Поддержка serverless функций (если понадобится для backend)

---

## 📝 Подготовка проекта к деплою

### 1. Создайте файл `netlify.toml` в корне проекта:

```toml
[build]
  base = "client"
  command = "npm install && npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

### 2. Настройка переменных окружения:

В Netlify Dashboard → Site settings → Environment variables добавьте:
- `REACT_APP_GA4_MEASUREMENT_ID=G-XXXXXXXXXX` (ваш Measurement ID)
- `REACT_APP_TMDB_API_KEY` (если используете)

### 3. Настройка API proxy (для backend):

Если ваш backend тоже нужно задеплоить, можно:
- Задеплоить на отдельный сервис (Railway, Render, Fly.io)
- Использовать Netlify Functions
- Использовать переменную окружения для API URL

---

## 🔧 Что сделать перед деплоем

### 1. Обновите API URL в production:

Создайте файл `client/src/config/api.ts`:

```typescript
export const API_URL = process.env.NODE_ENV === 'production' 
  ? process.env.REACT_APP_API_URL || 'https://ваш-backend.com'
  : 'http://localhost:5000';
```

### 2. Обновите `client/src/services/api.ts`:

```typescript
import axios from 'axios';
import { API_URL } from '../config/api';

export const api = axios.create({
  baseURL: API_URL,
  // ...
});
```

### 3. Убедитесь, что CORS настроен на backend для нового домена

---

## 🎉 После деплоя

1. Получите домен (например, `cinemood-123.netlify.app`)
2. Обновите Google Analytics:
   - Создайте новый Data Stream или обновите существующий
   - Укажите ваш домен: `https://cinemood-123.netlify.app`
3. Обновите `REACT_APP_GA4_MEASUREMENT_ID` в Netlify (Environment variables)
4. Redeploy

---

## 💰 Платные домены (когда понадобится)

Если захотите собственный домен (например, `cinemood.app`):

**Недорогие варианты:**
- **Namecheap**: ~$10/год для .com
- **Cloudflare Registrar**: цена по себестоимости
- **Google Domains**: ~$12/год

**Подключение к Netlify:**
1. Купите домен
2. В Netlify → Domain settings → Add custom domain
3. Следуйте инструкциям (добавьте DNS записи)

---

## 🚀 Быстрый старт (Netlify)

### Шаг 1: Подготовка проекта

Файл `netlify.toml` уже создан в корне проекта - он настроит деплой автоматически!

### Шаг 2: Задеплойте на Netlify

**Вариант A: Через GitHub (рекомендуется - автодеплой)**

```bash
# 1. Убедитесь, что проект на GitHub
git add .
git commit -m "Prepare for deployment"
git push

# 2. Зайдите на https://app.netlify.com
# 3. Нажмите "Add new site" → "Import an existing project"
# 4. Выберите "Deploy with GitHub" → авторизуйтесь
# 5. Выберите ваш репозиторий
# 6. Настройки заполнятся автоматически из netlify.toml!
# 7. Нажмите "Deploy site"
```

**Вариант B: Ручной деплой (без GitHub)**

```bash
# 1. Соберите проект локально
cd client
npm install
npm run build

# 2. Зайдите на https://app.netlify.com
# 3. Перетащите папку client/build в окно Netlify
# 4. Получите домен сразу!
```

### Шаг 3: Настройка переменных окружения

После деплоя в Netlify Dashboard:
1. Site settings → Environment variables
2. Добавьте:
   - `REACT_APP_GA4_MEASUREMENT_ID` = `G-XXXXXXXXXX` (ваш Measurement ID)
   - `REACT_APP_API_URL` = `https://your-backend.com/api` (когда задеплоите backend)
3. Redeploy site

### Шаг 4: Обновите Google Analytics

1. Откройте Google Analytics
2. Admin → Data Streams → ваш поток
3. Измените Website URL на ваш новый домен (например: `https://cinemood-123.netlify.app`)

**Готово! Через 2-3 минуты получите домен и сможете использовать его в Google Analytics! 🎉**

---

## 📝 Что дальше?

- ✅ Получили бесплатный домен
- ✅ Настроили Google Analytics с реальным доменом
- ✅ Можете поделиться ссылкой с пользователями

**Когда вырастет трафик** (и захотите свой домен):
- Купите домен (например, `cinemood.app` за ~$10/год)
- Подключите к Netlify (Site settings → Domain management)
- Netlify настроит SSL автоматически!

