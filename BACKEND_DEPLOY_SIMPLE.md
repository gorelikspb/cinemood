# 🚀 УЛЬТРА-ПРОСТАЯ ИНСТРУКЦИЯ: Деплой Backend на Railway

## ⚡ Быстрый вариант (5 минут)

### Шаг 1: Откройте Railway
1. Зайдите на **https://railway.app**
2. Нажмите **"Start a New Project"** (или "Login" если уже есть аккаунт)
3. Войдите через **GitHub** (кнопка "Login with GitHub")

### Шаг 2: Подключите репозиторий
1. Нажмите **"Deploy from GitHub repo"**
2. В списке репозиториев найдите **`cinemood`** (или `gorelikspb/cinemood`)
3. Нажмите на него

### Шаг 3: Настройте сервис
Railway автоматически определит проект, но нужно указать папку:

1. Откройте **Settings** (шестеренка справа)
2. Найдите **"Root Directory"**
3. Выберите или введите: **`server`**
4. Нажмите **"Save"**

Railway автоматически:
- ✅ Установит зависимости (`npm install`)
- ✅ Запустит сервер (`npm start`)
- ✅ Выдаст вам HTTPS URL

### Шаг 4: Настройте переменные окружения
В Railway Dashboard → **Variables** → **"New Variable"**:

Добавьте эти переменные (одну за раз):

1. **Name**: `CLIENT_URL`
   **Value**: `https://ваш-frontend-domain.netlify.app`
   (замените на ваш домен из Netlify)

2. **Name**: `NODE_ENV`
   **Value**: `production`

3. **Name**: `PORT`
   **Value**: `5000`

После добавления каждой переменной Railway автоматически перезапустит сервер.

### Шаг 5: Получите URL Backend
В Railway Dashboard вы увидите:
- **Домен**: `https://cinemood-production.up.railway.app` (или похожий)

**Скопируйте этот URL!** Он понадобится для настройки frontend.

---

## 🔗 Шаг 6: Подключите Frontend к Backend

### В Netlify Dashboard:

1. Откройте ваш сайт на Netlify
2. **Site settings** → **Environment variables**
3. Нажмите **"Add variable"**
4. Добавьте:
   - **Key**: `REACT_APP_API_URL`
   - **Value**: `https://ваш-railway-url.up.railway.app/api`
     (замените на URL из Railway, добавьте `/api` в конце)
5. Нажмите **"Save"**
6. **Deploys** → **Trigger deploy** → **Deploy site**

---

## ✅ Готово!

Теперь:
- ✅ Frontend на Netlify работает
- ✅ Backend на Railway работает
- ✅ Они подключены друг к другу!

**Проверьте:** Откройте ваш Netlify сайт и попробуйте добавить фильм. Должно работать! 🎉

---

## 🧪 Для тестов на локальном компьютере

Если хотите тестировать локально:

### Запуск локального backend:
```bash
cd server
npm start
```

### Запуск локального frontend:
```bash
cd client
npm start
```

Frontend автоматически подключится к `http://localhost:5000/api` (это настроено в `client/package.json`).

**Но помните:** Если компьютер выключен, backend будет недоступен. Для продакшена лучше использовать Railway.

---

## ❓ Что если что-то не работает?

1. **Backend не запускается:**
   - Проверьте логи в Railway Dashboard → **Deployments** → **View Logs**
   - Убедитесь, что в **Root Directory** указано `server`

2. **CORS ошибки:**
   - Проверьте, что `CLIENT_URL` в Railway = ваш Netlify домен
   - Проверьте, что `REACT_APP_API_URL` в Netlify = ваш Railway URL + `/api`

3. **404 ошибки:**
   - Убедитесь, что в Netlify переменная `REACT_APP_API_URL` заканчивается на `/api`

---

## 🎯 Итого: Что нужно сделать вручную

1. ✅ Зайти на Railway.app → войти через GitHub
2. ✅ Выбрать репозиторий `cinemood`
3. ✅ Указать Root Directory = `server`
4. ✅ Добавить 3 переменные окружения
5. ✅ Скопировать Railway URL
6. ✅ Добавить `REACT_APP_API_URL` в Netlify

**Всё! Остальное автоматически.** 🚀

