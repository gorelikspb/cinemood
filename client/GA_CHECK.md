# 🔍 Как проверить Google Analytics

## 1. Проверка в консоли браузера

Откройте консоль (F12) и выполните:

```javascript
// Проверка что gtag загружен
window.gtag
// Должно показать: ƒ gtag(){...}

// Проверка dataLayer
window.dataLayer
// Должно показать массив с данными

// Ручная отправка тестового события
window.gtag('event', 'test_event', { test: 'value' })
```

## 2. Проверка в Google Analytics

1. Откройте [Google Analytics](https://analytics.google.com/)
2. Выберите ваше свойство
3. Перейдите в **Realtime** → **Events**
4. Выполните действие в приложении (добавьте фильм, эмоцию и т.д.)
5. События должны появиться через 10-30 секунд

## 3. Включить дебаг-логи (временно)

Создайте файл `client/.env`:
```
REACT_APP_ANALYTICS_DEBUG=true
```

Перезапустите сервер. В консоли появятся логи:
- `📊 Track event: AddFilm {...}`
- `✅ Event sent to GA: AddFilm`

## 4. Проверка Measurement ID

Текущий ID: `G-4B5R6S0DLK`

Если нужно изменить, отредактируйте:
- `client/public/index.html` (строка 26 и 31)
- `client/src/config/analytics.ts` (строка 13)

## 5. Отслеживаемые события

- `AddFilm` - добавление фильма в дневник
- `AddEmotion` - добавление эмоции
- `OpenSimilar` - открытие похожего фильма
- `EmailSubmitted` - отправка email
- `AddToWatchlist` - добавление в watchlist
- `RemoveFromWatchlist` - удаление из watchlist
- `FeedbackSubmitted` - отправка фидбека


