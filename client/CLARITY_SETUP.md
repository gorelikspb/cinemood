# 🔍 Microsoft Clarity - Настройка и использование

## 1. Регистрация и получение Project ID

### Шаг 1: Регистрация
1. Откройте [https://clarity.microsoft.com](https://clarity.microsoft.com)
2. Войдите через Microsoft аккаунт (или создайте бесплатный)
3. Нажмите **"Create a new project"**

### Шаг 2: Настройка проекта
1. **Project Name**: введите название (например, "Cinemood")
2. **Website URL**: 
   - Для локальной разработки: `http://localhost:3000` (или другой порт, который использует ваш dev-сервер)
   - Для production: ваш реальный домен (например, `https://yourdomain.com`)
   - ⚠️ **Важно**: Clarity работает на localhost! Укажите `http://localhost:3000` чтобы видеть активность во время разработки
3. **Timezone**: выберите ваш часовой пояс
4. Нажмите **"Create"**

### Шаг 3: Получение Project ID
1. После создания проекта откроется страница с кодом
2. Найдите строку вида: `clarity('project', 'ABC123xyz')`
3. **Project ID** - это `ABC123xyz` (уникальная строка букв и цифр)
4. Скопируйте этот ID

### Шаг 4: Настройка в проекте
**✅ Project ID уже настроен:** `u0dil3s5nz`

Clarity включен по умолчанию в `client/src/config/analytics.ts`. Если нужно отключить, создайте файл `client/.env`:
```env
REACT_APP_CLARITY_ENABLED=false
```

**Перезапустите сервер разработки:**
```bash
cd client
npm start
```

После перезапуска Clarity автоматически подключится к вашему проекту.

---

## 2. Что можно смотреть в Clarity

### 📹 Recordings (Записи сессий)
1. В левом меню выберите **"Recordings"**
2. Вы увидите список записей реальных действий пользователей
3. Кликните на любую запись → откроется видео сессии
4. Можно увидеть:
   - Куда кликает пользователь
   - Как прокручивает страницу
   - Какие формы заполняет
   - Где застревает или ошибки

### 🗺️ Heatmaps (Тепловые карты)
1. В левом меню выберите **"Heatmaps"**
2. Выберите страницу из списка
3. Вы увидите:
   - **Click heatmap** - где чаще всего кликают (красные зоны = больше кликов)
   - **Scroll heatmap** - как далеко прокручивают страницу
   - **Rage clicks** - множественные клики (возможные проблемы)

### 📊 Dashboard (Панель)
1. Главная страница Clarity показывает:
   - **Active Users** - активные пользователи в реальном времени
   - **Popular Pages** - популярные страницы
   - **Insights** - автоматические находки (проблемы, паттерны)

### 🎯 Custom Events (Ваши события)
Ваши события (`AddFilm`, `AddEmotion` и т.д.) видны в:

1. **Recordings**:
   - Откройте любую запись
   - В правой панели найдите раздел **"Custom Events"**
   - Там будут события, которые произошли в этой сессии

2. **Filtering**:
   - В списке Recordings нажмите **"Filters"**
   - Выберите **"Custom Events"**
   - Найдите `AddFilm`, `AddEmotion` и т.д.
   - Покажет только сессии, где происходили эти события

3. **Analytics**:
   - В меню **"Analytics"** → **"Custom Events"**
   - Статистика по вашим событиям

---

## 3. Полезные фильтры

### Найти сессии с конкретным событием:
1. **Recordings** → **Filters**
2. **Custom Events** → выберите `AddFilm`
3. Увидите только записи, где пользователи добавляли фильмы

### Найти проблемные места:
1. **Heatmaps** → выберите страницу
2. Посмотрите на **Rage clicks** (красные зоны) - множественные клики
3. Возможно, кнопка не работает или интерфейс непонятен

---

## 4. Советы

- ✅ Clarity записывает только реальные действия пользователей (не боты)
- ✅ Данные появляются через несколько минут после действий
- ✅ Бесплатно до 100,000 страниц просмотров в месяц
- ✅ Все записи хранятся 12 месяцев

---

## 5. Как увидеть активность на Localhost

### ✅ Clarity работает на localhost!

**Важно**: При создании проекта в Clarity укажите `http://localhost:3000` в поле **Website URL**. Clarity будет отслеживать активность на localhost.

### Шаги для проверки:

1. **Убедитесь, что Clarity подключен:**
   - Откройте браузер → `http://localhost:3000`
   - Откройте DevTools (F12) → вкладка **Network**
   - Обновите страницу (F5)
   - Найдите запрос к `clarity.ms` - должен быть запрос с вашим Project ID

2. **Проверьте в консоли браузера:**
   - Откройте DevTools → вкладка **Console**
   - Если Clarity загружен, увидите что-то вроде: `[Clarity] Initialized`
   - Если ошибок нет - всё работает!

3. **Создайте активность:**
   - Покликайте по странице
   - Заполните формы
   - Перейдите на другие страницы
   - Вызовите события (добавьте фильм, эмоцию и т.д.)

4. **Проверьте в Clarity Dashboard:**
   - Откройте [https://clarity.microsoft.com](https://clarity.microsoft.com)
   - Выберите ваш проект
   - Подождите **2-5 минут** (данные появляются с задержкой)
   - Перейдите в **Recordings** → увидите записи ваших действий
   - Перейдите в **Heatmaps** → увидите тепловые карты кликов

### 🔍 Где искать данные:

- **Dashboard** → **Active Users** - активные пользователи (включая вас на localhost)
- **Recordings** → список всех записей (включая сессии с localhost)
- **Heatmaps** → выберите страницу `http://localhost:3000/...` из списка
- **Analytics** → **Custom Events** → ваши события (`AddFilm`, `AddEmotion` и т.д.)

### ⚠️ Важные моменты:

- Данные появляются с задержкой **2-5 минут** (иногда до 10 минут)
- Каждая сессия = открытие браузера и действия на сайте
- Чтобы увидеть новую сессию, закройте все вкладки с localhost и откройте заново
- Clarity автоматически различает разные сессии

---

## 6. Troubleshooting

**Не вижу данные на localhost?**
- ✅ Проверьте, что в настройках проекта Clarity указан `http://localhost:3000`
- ✅ Проверьте, что Project ID правильный (`u0dil3s5nz`)
- ✅ Перезапустите сервер разработки (`npm start`)
- ✅ Обновите страницу в браузере несколько раз (чтобы создать сессии)
- ✅ Подождите 5-10 минут (данные появляются с задержкой)
- ✅ Проверьте Network в DevTools - должен быть запрос к `clarity.ms`

**Не вижу кастомные события?**
- Убедитесь, что в коде вызывается `track(AnalyticsEvents.AddFilm, ...)`
- Проверьте консоль браузера на ошибки
- В Clarity → Recordings → откройте запись → Custom Events
- Подождите несколько минут после события


