# 🧩 Переиспользуемые Компоненты

## MovieSearch

**Файл:** `client/src/components/MovieSearch.tsx`

**Назначение:** Компонент для поиска фильмов через TMDB API с debouncing и кэшированием.

**Использование:**
- `AddMovie.tsx` - основной поиск при добавлении фильма
- Потенциально в других местах где нужен поиск фильмов

**Ключевые особенности:**
- Debouncing (задержка 300ms)
- React Query для кэширования
- Настраиваемый placeholder и максимальное количество результатов
- Callback `onMovieSelect` для обработки выбора

## MovieForm

**Файл:** `client/src/components/MovieForm.tsx`

**Назначение:** Переиспользуемая форма для ввода данных о просмотренном фильме.

**Использование:**
- `AddMovie.tsx` - при добавлении нового фильма (дата сверху)
- `MovieDetails.tsx` - при редактировании существующего (дата снизу)

**Поля формы:**
1. **Рейтинг** - слайдер 1-10 с эмодзи обратной связью
2. **Заметки** - текстовое поле для мыслей о фильме
3. **Дата просмотра** - date picker
4. **Эмоции** - сетка кнопок с эмодзи
5. **Описание эмоций** - текстовое поле (показывается если выбраны эмоции)

**Настройки:**
- `showDateFirst` - порядок полей (дата сверху или снизу)
- Все обработчики передаются как props для максимальной гибкости

**Преимущества выделения:**
- ✅ Единообразный интерфейс в разных местах
- ✅ Легче поддерживать изменения
- ✅ Переиспользование логики отображения
- ✅ Централизованное управление стилями

## MovieSearch + MovieForm = Полный цикл

Эти два компонента покрывают весь цикл работы с фильмами:
1. **MovieSearch** - найти фильм
2. **MovieForm** - оценить и добавить в дневник

Оба компонента используют:
- Единые константы стилей (`STYLES`)
- Единые данные эмоций (`EMOTION_TYPES`, `getEmotionData`)
- Переводы через `useTranslation`
